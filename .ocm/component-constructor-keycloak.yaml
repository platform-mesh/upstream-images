components:
  - name: {{ .COMPONENT_NAME }}
    version: {{ .IMAGE_VERSION }}
    provider:
      name: The platform-mesh Team
    resources:
      - name: chart
        type: helmChart
        relation: external
        version: {{ .CHART_VERSION }}
        access:
          type: ociArtifact
          imageReference: "{{ .CHART_OCI_PATH }}:{{ .CHART_VERSION }}"
      - name: image
        type: ociImage
        relation: external
        version: {{ .IMAGE_VERSION }}
        access:
          type: ociArtifact
          imageReference: "{{ .IMAGE_NAME }}:{{ .IMAGE_VERSION }}"
    sources:
      - name: source
        type: git
        version: {{ .IMAGE_VERSION }}
        access:
          commit: {{ .IMAGE_REPO_SHA }}
          repoUrl: {{ .IMAGE_REPO }}
          type: gitHub
      - name: chart
        type: git
        version: {{ .CHART_VERSION }}
        access:
          commit: {{ .COMMIT }}
          repoUrl: {{ .CHART_REPO }}
          type: gitHub
